fastapi>=0.110.0
uvicorn[standard]>=0.28.0
pydantic>=2.6.0
httpx>=0.27.0
python-dotenv>=1.0.0
python-multipart>=0.0.9

# Gemini through the Google Gen AI SDK and the Parallel Search API — the two runtime
# services this project is judged on actually calling.
google-genai>=0.1.1
parallel-web>=0.1.0

# Screenplay upload: PDFs are parsed with pypdf; .docx is read as a zip by
# app/script_upload.py, so no Word library is needed.
pypdf>=5.0.0

# Frame simulator. Pillow pre-crops for telephoto; opencv+numpy back
# app/viewpoint_check.py, which measures whether the camera really moved.
# The *headless* build is required — the default opencv-python links libGL,
# which python:slim does not ship, and the container dies at import.
pillow>=10.0.0
numpy>=1.26.0
opencv-python-headless>=4.9.0

# Tests only, kept here so the image can run them in CI.
pytest>=8.0.0
pytest-asyncio>=0.23.0
